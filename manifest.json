{
  "manifest_version": 2,

  "name": "Daily Planner",
  "description": "This extension allows the user to replace their new tab with a daily planner page including a to-do list, calendar, the weather, time etc.",
  "version": "1.0",

  "background": {
    "scripts": ["background.js"]
  },

  "browser_action": {
    "default_icon": "icon.png",
    "default_title": "Pablo's Daily Planner!"
  },

  "chrome_url_overrides": {
      "newtab": "popup.html"
  },

  "permissions": [
    "activeTab",
    "storage",
    "identity",
    "cookies"
  ]
}
